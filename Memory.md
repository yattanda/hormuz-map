# Memory.md — ホルムズ海峡危機マップ 引き継ぎドキュメント

最終更新: 2026-09-23（確認期間を 10/2 の一律解禁からゲート A・B・C 方式へ変更）
（前々回: 2026-09-21 将来の構成方針〔複数チョークポイント化・リポジトリ名〕と新テーマの土台を記録）
（前回以前: 2026-09-19 ドメイン移行完了〔2026-09-18〕・フェーズ2 CSS の本番反映・AIS 見送り・iframe 高さ同期の現状を反映）

---

## プロジェクト概要

- **サイト**: https://chokepointlab.com/
- **構成**: `docs/index.html`（HTML/CSS/JS全集約）+ `docs/data/news_data.json`
- **更新頻度**: 毎日〜数日に1回
- **目標**: 継続可能な媒体化（信頼性の確立 → 独自ドメイン移行 → 収益化）。※媒体名・ドメイン・法務・収益化などの上流の方針、および三媒体連携の構想はリポジトリ外の戦略文書が正本。この Memory.md は技術・実装の現状に限定する

---

## 現在の状態

- **公開中**: GitHub Pages で稼働中（独自ドメイン `https://chokepointlab.com/`）。日次更新を継続中
  - 【2026-09-18】独自ドメインへ移行完了（Custom domain 設定 `75bbdac` → ホスト名置換 `60b62a1`、Enforce HTTPS 有効）。
    旧 URL `https://yattanda.github.io/hormuz-map/` は新ドメインへ 301 転送。
    本番検証は基準値どおり（`tools/migration-baseline-2026-09-13.md` §8）
  - ダッシュボード（`hormuz-data-`）は `yattanda.github.io/hormuz-data-/` のまま。**本体とは別オリジンになった**
- **現フェーズ**: ~~基盤整備（法務ページ整備・独自ドメイン移行準備）~~
  → 【2026-09-18〜】**移行後の確認期間**。~~〜10/2頃・HTML の構造・パス・タイトルは変えない~~
  → 【2026-09-23 変更】**日付ではなくゲート A・B・C で区切る**（`tools/redesign-plan.md` §1「確認期間のゲート」）。
  A（9/23〜）パス・タイトル・メタを変えない作業／B（9/25〜26 頃の点検後）フェーズ1b の残り／
  C（9/30 頃の点検で判定）パスを変える再編。日常のコンテンツ更新は継続
  - 例外として、**フェーズ2のうち CSS だけの変更（2-a 文字サイズ・2-b 行の長さ・2-c カラム／ブレークポイント）は
    2026-09-18 に本番反映済み**（確認期間中に前倒し。`<style>` 内のみ。詳細は `tools/redesign-plan.md` §4「実施結果」）
  - ~~10/2 以降~~ → ゲート順：フェーズ2の HTML 側の残り（注記箱へのクラス付与・特別解説コラムの入れ子修正・インライン `font-size` のクラス化）
    → フェーズ1b の残り（セクション統合＋S03・S08.5）→ 再編（§11-10）→ 表示の統一（基準日・主要数値の静的化など）→ フェーズ3
- **並行して進行中**: ~~日次更新の Claude Code 主体化（2026-09-02 着手・フェーズ0〜2完了。旧経路はバックアップとして保持）~~
  → 移行は実質完了（フェーズ0〜2完了・クラウドセッションが主経路）。旧経路はバックアップとして保持（2026-09-14 決定）

---

## 現在進行中の課題（優先順位順）

### 🟡 重要（順次対応）

#### 1. ~~出典・更新ログの整理~~ ✅ 完了（2026-05-07）
- 2カラム統合・最新3件常時表示・4件目以降折り畳み実装済み
- 全27件を `docs/data/update_log.json` に切り出し、index.htmlは最新10件のみ保持

#### 2. ~~hormuz-data- iframe 空白問題（⏸️ 保留中・根本解決待ち）~~ → 固定値方式は廃止済み（2026-09-02）

> 【2026-09-19 追記】この節は **2026-05-08 時点の記録**。2026-09-02 に固定高さをやめ実コンテンツ高さを測る方式へ変更し
> （commit `2c903a1` `075ac2f`）、以下の「現在のリポジトリ値」は**現状と異なる**（現在の固定値はフォールバック専用で
> PC 1110px／〜768px 2200px／〜480px 2110px）。独自ドメイン移行後の状態は下の
> 「保留中の課題 › ダッシュボード iframe の postMessage 高さ通知」を参照。以下は経緯として残す

**症状**:
- PC: iframeコンテンツ終端〜USOILウィジェットの間に大きな黒い空白
- スマホ: さらに深刻で、実機ではF12シミュレーションの約2倍の空白が発生

**根本原因**:
- `adjustIframeHeight()` がスマホで `2200px` 固定を設定しているが、
  実際のiframeコンテンツ高さが環境・DPRにより可変のため、常にずれが生じる
- 固定値で合わせようとすると「大きすぎて空白」か「小さすぎてコンテンツが切れる」の繰り返しになる

**2026-05-08 試行記録**（固定値アプローチ・失敗）:

| バージョン | スマホ(≤480px) | PC | 結果 |
|---|---|---|---|
| 元の値 | 2200px | 1000px | 空白が巨大 |
| v1 | 1200px | 800px | コンテンツが切れた |
| v2 | 1500px | 800px | まだ切れた |
| v3 | 1700px | 950px | PCほぼ完成、スマホまだ切れた |
| v4 | 1900px | 950px | もう少し切れていた |
| v5 | 2110px | 1110px | pullで元の値に戻り失敗 ← ここで保留 |

**失敗の構造**:
- pullにより `adjustIframeHeight()` の値がリポジトリの元の値（2200px/1000px）に戻る
- 固定値の手動チューニングは恒久解決にならない

**次回の対応方針（保留中）**:
- **アプローチA（推奨・低リスク）**: `hormuz-data-` 側の `body/html` の `min-height` を確認・削除し、
  実コンテンツ高さを小さくする → その上でフォールバック値を設定
- **アプローチB（根本解決・中リスク）**: postMessageによる動的サイジング
  - 過去に試みたが真っ黒になりリバート済み
  - 次回は `hormuz-data-` 側の `body/html min-height` 削除を先に行ってから再挑戦
- **前提確認事項**: `hormuz-data-` の `index.html` で `body { min-height: 100vh; }` 等の設定がないか確認すること

**現在のリポジトリ値**（元に戻っている状態）:
```javascript
if (window.innerWidth <= 480) {
  iframe.style.height = '2200px';   // ← 空白が大きい
} else if (window.innerWidth <= 768) {
  iframe.style.height = '2300px';
} else {
  iframe.style.height = '1000px';   // ← クリップが発生
}
```

#### 3. スマホUI改善（継続課題）

**現状の問題点**:
- スマホ表示で余白が広すぎる箇所がある（`margin-bottom` / `gap` / `padding` の過剰）
- 見出しサイズが画面幅に対して大きすぎる場合がある
- セクション間の間延びがある
- 表の列幅がスマホで最適化されにくい
- Claudeの修正が試行錯誤になりやすかった（→ 専用エージェント・ルールで解決済み）

**採用する改善方針**:
- 375pxを最重要基準として確認する
- モバイルファーストでCSSを整理する
- 見出し・余白には `clamp()` を優先する
- 表は横スクロールまたはカード型を基本にする
- 共通CSSで直せるものを優先し、ページ固有の場当たり的CSSを増やさない
- 修正時は `mobile-ui-reviewer` → `responsive-css-specialist` の順で進める
- 詳細ルールは `tools/design-system.md` / `tools/mobile-ui-rules.md` を参照

#### 4. インフォグラフィック導線改善
- **対象**: 30秒カラム末尾の「📊 解説インフォグラフィック」→「ホルムズ海峡危機を6枚の図解で俯瞰する」
- **問題**: タイトルの色が地味、大タイトルからの導線が不明瞭
- **方針**: タイトル色を青系（`#38bdf8` or `#43d9ff`）に変更、ボタン/バナーとして視認性強化

---

### 🟢 機能追加（余裕があれば）

#### 4. キーワードツールチップ
- **概要**: 重要キーワード上にカーソルオン（スマホはタップ）で簡潔な解説ポップアップ
- **対象語例**: ホルムズ海峡、OPEC、Kpler、CENTCOM、OFAC、BPD 等
- **実装方針**: CSS `position:relative` + `::after` または `<span data-tooltip="...">` + JSで制御
- **スマホ対応**: タップで表示・もう一度タップで非表示

---

## 保留中の課題

### ✅ ダッシュボード「封鎖前比」の分母 — 解決済み（2026-09-05 記録 → 2026-09-05 解決）

依頼4で「分子と分母で前提が 2.5 倍ずれており割合として読めない」として保留にしたが、
依頼5 Phase 2a で決着した。**この項目に残作業は無い。**

- 分母を **200 → 100隻/日**に是正し `verified: true` にした
  （`hormuz-data-/data/context.json` の `normal_vessels_per_day`。出典は Kpler・UNCTAD を
  参照した Al Jazeera 報道。commit `25e0bc0`）
- `scripts/fetch_manual.py` のプロンプトも同じ値を参照するようにし、分子と分母の前提を一本化
  （出力レンジの誘導自体を撤去。commit `fd693fa`）
- 従来の 200 は実態のおよそ2倍で、**封鎖前比を半分に過小表示していた**
- 残る改善余地は `context.json` の `todo` に記載（IMO の月次通航データを読み取れれば公式系の出典に置き換える）

### ✅ 反映済み（2026-09-22）— イラン・GCC 外相会合の日付（2026-09-20 記録）

**2026-09-22 に反映した。** 「報道に幅があり断定できない」は1か所ではなく4か所（index.html の速報欄・本文カード・サマリー、news_data.json）にあり、すべて「（9/22 追記）」を明示して確定内容に書き換えた。9/22 の日次更新本文の「9月14日ごろ」も直した。更新扱いで訂正履歴には載せていない。**この項目に残作業は無い。**


`docs/index.html:1739`（9/15 の速報欄）に、当初予定日について

> 当初予定日は報道に幅があり（NPR等は9/14、France24は9/16と報道）断定できない（NPR、France24）

と書いているが、**その後の確認で 9/14 に確定できる**。あわせて**延期が発表された日（9/13）**も
書けるようになった。現在の本文は「開催前に延期された」とだけで、発表日を示していない。

- 決め手：オマーンのバドル・アルブサイディ外相本人が **9/13** に
  「合意形成のため、**明日**サラーラで予定されていた地域会合は延期された」と発表
  （[RFE/RL（globalsecurity.org 経由）](https://www.globalsecurity.org/wmd/library/news/iran/2026/09/iran-260913-rferl01.htm)）
- CNN（9/13）・Washington Post（9/14）・RFE/RL も 9/14 で一致。France24 の「9/16」は裏が取れない
- 開催地「サラーラ」は**正しい**（同じブサイディ外相の発言）。Al Jazeera 9/12 の「マスカット」は
  イラン外務省発表を引いた早い段階の報道で、開催国の確認ではなかった。**訂正不要**

**扱い**：掲載時点では実際に報道が割れており、当時の慎重な書き方は誤りではない。
その後の情報で確定したので**「更新」**（訂正の方針 §1）。**訂正履歴には載せない。**

**やること**：次の日次更新で `docs/index.html:1739` の当該箇所を
「9月14日にサラーラで予定されていた会合が、9月13日に延期された」の趣旨に書き換える。
調査の記録は `chokepointlab-notes/2026_9_20_記述の食い違い確認.md` §1・§2。

### ⏸️ ルートテーブル スマホ右余白
- `<colgroup>` を変更するとテーブルが崩壊することを確認済み
- 複数回試みたが未解決 → 次回は colgroup を触らない別アプローチで検討

### ✅ ダッシュボード iframe の postMessage 高さ通知 — 解決（2026-09-02 記録 → 2026-09-18 更新 → 2026-09-19 解決）

**【2026-09-19 本番で目視確認・解決扱い】空白は戻っていない。**
- 本番トップを 1280×800 と 375×812（スマホ）で開き、iframe の下端までスクロールして目視した。
  **どちらも、ダッシュボード末尾（データソース・各データの基準日・免責）の直後に「💰 リアルタイム市場」が続き、空白は無い**。
  375px で横スクロールも無し（`scrollWidth` 375）
- iframe 高さは **PC 1445px／スマホ 2291px**（9/18 の 1476／2338 と近い。日々の内容量で変わる）。
  9/2 の期待値（720／1640）との差は、**9/2 以降にダッシュボード末尾へデータソース一覧・基準日一覧・免責が加わった分**と目視で整合する
  （→ 期待値の側が古かった。確度：中〜高）
- 注意（計測の落とし穴）：トップの `docs/index.html` 自身にも postMessage の送信スクリプト（末尾 `postMessage height sender`）があり、
  埋め込まれていない状態では**自分自身へ自ページの高さ（約 18,000px）を送る**。下記 1 のスニペットで拾うと紛らわしいが、
  親の受信側は `ev.source !== iframe.contentWindow` で捨てているので実害は無い
- 注意：ブラウザの表示枠が非表示（幅 0）のまま測ると、iframe が 1 万 px 超に伸びた値が出る。必ず幅を指定して測ること

**【2026-09-18 移行後の実測】postMessage 経由の高さ同期が初めて動作した**（`tools/migration-baseline-2026-09-13.md` §8）。
ただし解決済みとはしない。事実を並べると次のとおり。

| 項目 | 内容 |
|---|---|
| 移行後の iframe 高さ（9/18 実測） | **PC 1476px／スマホ 2338px**。フォールバック値（1110 / 2200 / 2110）ではない＝postMessage の値が適用されている |
| 下記「やること」2 の期待値（9/2 の同一オリジン実測） | PC 720px 前後（1400px 幅）／スマホ 1640px 前後（375px 幅） |
| 差 | PC 約 +755px、スマホ 約 +700px。**期待値と一致していない** |
| 測定条件 | 9/18 の測定幅は §8 に記載が無い（§3 の基準値測定は 1280px／375px）。9/2 とは条件が揃っていない可能性がある |
| 子側の測り方 | `hormuz-data-/index.html` の送信スクリプトは親の `measureInner()` と同じ「body 直下要素の下端」方式（`documentElement.scrollHeight` ではない）。異常の見分け方①には該当しない |
| 空白が戻っていないか | **不明**。本番での目視確認の記録が無い（`tools/` の記録・引き継ぎノートのいずれにも無い） |

- 差の原因は**未検証**。9/2 以降のダッシュボード側の内容増加（footer のデータソース一覧など）や測定幅の違いで
  説明できる可能性はあるが、確かめていない（推測を事実として扱わないこと）
- **次にやること**：PC・スマホ幅で本番トップを開き、下記 1 のスニペットで受信値と iframe 高さを見る。あわせて
  iframe 内の最終要素の下端と iframe 下端の間に空白が無いかを目視する。空白が無ければ期待値の側を更新して解決扱いにする

以下は 2026-09-02 時点の記録（経緯として残す）。

**現状（2026-09-02 時点）：表示は直っている。ただし postMessage 経路だけ未検証。**

- `docs/index.html` のダッシュボード iframe は、固定高さ（PC 1110px / スマホ 2110〜2200px）をやめ、
  **iframe 内の body 直下要素の下端から実コンテンツ高さを測る方式**に変更済み
  （2026-09-02 / commit `2c903a1` `075ac2f`、push 済み）
- 本番実測：PC 1400px 幅で 1110px → **721px**、スマホ 375px 幅で 2110px → **1644px**。
  約 390〜466px の空白が解消（残 9px は意図的な下マージン）
- 実測が成立するのは `hormuz-map` と `hormuz-data-` が **どちらも yattanda.github.io ＝ 同一オリジン**だから
- 独自ドメイン移行後は別オリジンになり `contentDocument` を読めなくなるため、
  子（`hormuz-data-/index.html`）から `postMessage({type:'iframeHeight', height})` を送る経路を用意し、
  親側に受信処理を実装済み。**子側の送信スクリプトも投入済み（2026-09-02）**

**なぜ今は検証しないか（意図的な保留）**

- 親側に `directMeasureOk` ガードを入れてあり、**同一オリジンで実測できている間は postMessage を無視する**。
  子側が誤った高さを送っても現在の表示が壊れない設計
- その代償として、**移行前は送信スクリプトの実挙動を確認できない**。DevTools での事前確認は行わず、
  ドメイン移行後に確認する方針とした

**ドメイン移行後にやること**

1. 移行後のトップページを開き、DevTools のコンソールで受信値を観測する

   ```javascript
   const f = document.getElementById('hormuz-dashboard');
   window.addEventListener('message', e => {
     if (e.data?.type === 'iframeHeight')
       console.log('受信:', e.data.height, '/ iframe:', f.getBoundingClientRect().height);
   });
   ```

2. 再読み込みし、**受信値と iframe 高さがほぼ一致**すること（PC 720px 前後 / スマホ 1640px 前後）を確認
3. **異常の見分け方**
   - 受信値が 1110 や 2110（＝固定値そのもの）→ 子側が `documentElement.scrollHeight` を使っている。
     iframe 内ではこの値が iframe の高さまで引き伸ばされて返るため縮小判定ができない。
     子側も親と同じ「body 直下要素の下端」方式に直す
   - 何も受信しない → 子側スクリプトが未反映、または `window.parent === window` 判定で早期 return している
   - 受信がないまま固定値のまま表示 → フォールバック動作。崩れはしないが余白は戻る

---

## 今後の最重要課題（次フェーズ）

- ~~**基盤整備（現在進行中）**: 法務ページ整備・独自ドメイン移行~~ → 【2026-09-18】法務ページ整備・独自ドメイン移行とも**完了**。
  現在は移行後の確認期間（2026-09-23 からゲート方式）。詳細な方針・手順は上流の戦略文書が持つ（この文書には固有名詞・戦略詳細を書かない）
- **サイト大改造 フェーズ2の残り（HTML 側）**：~~10/2 以降~~ → ゲート A（9/23〜）で着手可（`tools/redesign-plan.md` §4「10/2 以降に回したもの」）
- **ダッシュボード iframe 高さの確認**：移行後の実測値が期待値と一致していない（上記「保留中の課題」参照）
- **日次更新の Claude Code 主体化**（2026-09-02 着手・実質完了）→ 下記セクション参照
- **Xへの投稿自動化**（別スレッドで対応中）
- ~~**AIS データの実測化**（2026-09-03 実装完了・有効化待ち）~~ → 【2026-09-05】カバレッジ不足のため**見送り**。
  当面は AI 推計のみ（出自を明示）で運用。下記セクション参照
- **【2026-09-21 頭出し】新テーマ：YouTube 視聴者をサイトへ誘導する導線とサイト大改造**。
  先に土台（GA4・UTM・直リンク化）を整備する。方針の詳細は上流の戦略文書側。下記セクション参照

---

## 将来の構成方針 — 複数チョークポイント化とリポジトリ名（2026-09-21 記録）

### 決定事項（運営者の方針）

- 新しいリポジトリは作らず、**1ドメイン＝1サイトリポジトリ**に集約する
- チョークポイントは**サブディレクトリ**で分ける（例：`docs/taiwan/` → `chokepointlab.com/taiwan/`）
- 別のチョークポイントをメインに昇格させる時点で、リポジトリ名を `chokepointlab` 等へ変更する
- 既存ページの URL は極力変えない。移動する場合は旧 URL に meta refresh＋canonical の転送ページを置く
  （GitHub Pages では 301 を出せないため。Google は即時の meta refresh を恒久的な転送として扱う）
- 大改造・構造変更・リポジトリ名の変更は**ブランチ＋PR** で行う（CLAUDE.md「ブランチルール」、`d508a87`）

### ルートと `/hormuz/` の関係 — 2026-09-19 に確定済み（案C）

- **ルート `/` は媒体トップ（ハブ）にし、現在のトップの中身は `/hormuz/` へ移す。**
  ホルムズ以外は当面、地図内の付随情報にとどめ、テーマページは中身を継続して更新できるまで作らない
- 実施は §11-10（~~10/2 以降~~ → 2026-09-23 から**ゲート C 成立後**・設計はいまから並行。順序は GA4 全ページ展開〔済〕 → フェーズ2 HTML の残り → フェーズ1b の残り〔本体〕 → §11-10 → 表示の統一）。
  判断の理由・比較した案・受け入れたリスクは上流の戦略文書（§4-6）が正本
- 【訂正】2026-09-21 の初版（`6614451`）ではこの点を「未決・案A推奨」と書いていたが、
  9/19 の決定を確認しないまま書いた誤り。同日中に訂正した

### §11-10 の実施条件（2026-09-21 追加。案C は据え置き）

1. **§11-10 が済むまで、YouTube 等の外部に貼るリンクはルート（`https://chokepointlab.com/`）＋UTM のみ。**
   ルートは移設後もハブとして同じ URL で残る。記事・`#アンカー` への直リンクは URL が固まってから
   - 理由：meta refresh の転送先は固定 URL のため、**クエリ（UTM）と `#アンカー` が落ちる**（確度：高）
2. **旧パスのスタブはクエリとアンカーを引き継ぐ形にする。**
   `location.replace(新URL + location.search + location.hash)` を基本に、0秒 meta refresh と canonical を併記
3. **PR の切り方**：ルートの `/hormuz/` への移設・ハブ新設・スタブ設置・日次更新経路の修正は**1つの PR にまとめる**
   （分けると途中でトップが空になるか日次更新が壊れる）。`docs/index.html` を名指ししている箇所は
   14ファイル（2026-09-21 実測：スキル・`apply_diffs.py`・`mobile-update.yml`・`validate_daily.py` 等）。
   マージはその日の日次更新の後。記事のディレクトリ化は別 PR。リポジトリ名（ホスト）の変更とは混ぜない
4. **他リポジトリ側の追従**：`hormuz-data-/index.html` の戻りリンク（2か所）を `https://chokepointlab.com/hormuz/` へ。
   `/corrections/` の `href="../"` は `../hormuz/` へ（注記済み）。他の法務ページ6枚の `../` はハブを指すのが正しいので据え置き
5. **（2026-09-22 追加）ハブは `?focus=` 付きのアクセスをクエリごと `/hormuz/` へ送る。**
   `location.replace('/hormuz/' + location.search + location.hash)` 相当。これが無いと、それまでに YouTube に貼った
   `https://chokepointlab.com/?focus=…` が地図に届かなくなる。トップの「YouTube から来た方へ」枠もハブへ引き継ぐ

### 実施時の注意（2026-09-21 実測）

- `docs/index.html` の GA4 タグは Search Console の所有権確認に使用中。**ルートに置くページの `<head>` に残す**（CLAUDE.md）
- `docs/CNAME` は公開元の `docs/` 直下に置いたまま動かさない
- リポジトリ名の変更で追従が要る箇所：
  - `auto_push.py:39`（`"repo": "hormuz-map"`）、`.env` の `GITHUB_REPO`
  - **`hormuz-data-/index.html` の189行目と421行目**：メインサイトへの戻りリンクが旧 URL `yattanda.github.io/hormuz-map/` のまま。
    今は 301 転送で届いているが、名前を変えると届かなくなる見込み（確度：中）。**名前の変更とは別に、先に `https://chokepointlab.com/` へ直しておくのが安全**
  - ローカルの接続先：`git remote set-url origin https://github.com/yattanda/<新名>.git`
  - ローカルのフォルダ名も変える場合、Claude Code のメモリと設定の保存先（フォルダのパスで決まる）が切り替わり、引き継がれない
- 名前の変更後の見込み：独自ドメインでの配信は継続（確度：中〜高）。旧 URL `yattanda.github.io/hormuz-map/` は転送されなくなる可能性（確度：中）。
  変更後に本番と旧 URL の両方を実測する。Search Console の旧 URL プロパティへの影響も確認する

### 新テーマの土台（先行して整備する）

- **GA4 は 2026-09-22 に `docs/` 配下の全15ページへ展開済み**（`b146be4`〜`f75f484`）。`hormuz-data-` は未設置（④ で判断）
  - `/privacy/` 第6章の設置範囲・最終更新日も同じ作業で改訂した（`b146be4`）
  - 確認期間中だが、`<head>` へのタグ追加は構造・パス・タイトルを変えないため実施した
  - **新しいページを作るときは、`<head>` の viewport の直後に同じ GA4 タグを入れる**（入れないと `/privacy/` の「すべてのページに設置」と食い違う）。
    記事は `tools/article-template.html` に入れてあるので、テンプレートからコピーすれば入る
- **UTM**：YouTube のスマホアプリからの流入は参照元が落ちて direct に紛れやすい（確度：中〜高）。
  概要欄のリンクには UTM を付ける。**命名規則は 2026-09-22 に決定**：
  `?utm_source=youtube&utm_medium=video&utm_campaign=<公開日YYYYMMDD>-<短い英語>&utm_content=<description|pinned_comment|community|channel_links>`
  （すべて小文字。サイト内リンクには付けない。台帳は上流の非公開ノート側）
- **`?focus=<キー>`（2026-09-22 実装、`ca84606`）**：トップの地図を指定の印へ寄せてポップアップを開く。UTM ではなく表示を決めるパラメータ。
  書く順は `?focus=<キー>&utm_…`。キーは小文字英字で、`docs/index.html` の `FOCUS_TARGETS`（5-C）に定義する。
  現在のキー：`minamitori`（南鳥島）。キーを足したら、トップの「YouTube から来た方へ」枠（`#yt-visitors`）と台帳にも足す。
  `#アンカー` を使わないのは、§11-10 実施条件1に反し、GA4 の page_location にも残らないため
- **直リンク化**：リンク先となるアンカー（`id`）の棚卸しが必要

---

## AIS 通航船舶数 — 実測と推計の分離・VesselAPI 移行（2026-09-03）→ 実測は見送り（2026-09-05）

> **【2026-09-05 決定】AIS 実測は当面断念し、「AI 推計のみ・出自を明示して運用」とする。**
> VesselAPI は規約上の許諾は得られたが、**実際の通航レーン（東経 56.1 度以東）のカバレッジが薄く**、
> 得られる数値が海峡の交通量ではなく提供元のカバレッジ範囲を表すものになるため、有効化を見送った
> （有料プランでも解決しない）。見送りの理由は規約ではなくデータの妥当性。
> 実装（実測／推計の分離・実測0隻の三値化）は残し、`update_ais.yml` は `if: false` のまま維持する。
> 一次情報は `hormuz-data-/tools/README.md`「採用は見送った（2026-09-05 決定）」「確定した方針」。
> 以下のうち「データソース」「残件」は 2026-09-03 時点の記述で、残件は下で更新している。

### 結論

**ダッシュボードの「ホルムズ海峡 通航船舶数」は、これまで実測ではなく Gemini の推計だった。**
実測と推計を別ファイルに分離し、読者に出自が分かる表示へ是正した。実装・push 済み
（`hormuz-data-` commit `c8f9636`）。**hormuz-map 側の変更は不要**（AIS を直接参照していないため）。

### 何が起きていたか

- `data/ais-snapshot.json` を **2つのワークフローが交互に上書き**していた
  （`update_ais.yml` 09:00 JST が実測、`update_manual.yml` 09:30 JST が Gemini 推計）
- 実測と推計が **同名フィールド `vessels_detected`** に入り、判別は `source` 文字列の部分一致1箇所のみ
- さらに AISstream.io には**ペルシャ湾のカバレッジが存在せず**、2026-04-15 から約4.5か月
  `0 隻` を書き続けていた（2026-09-01 に判明し `update_ais.yml` を無効化）
- 結果として、表示されていたのは**実質すべて Gemini 推計**だった

### 是正内容

| ファイル（`hormuz-data-`） | 役割 |
|---|---|
| `data/ais-snapshot.json` | **実測専用**。`scripts/fetch_ais.py`（VesselAPI）のみが書く |
| `data/ais-estimate.json` | **推計専用**。`scripts/fetch_manual.py`（Gemini）のみが書く |

- 出自の判別は `source` 文字列ではなく **`measurement`（`"actual"` / `"estimate"`）** フィールド
- 表示側 `loadAIS()` は実測を優先し、条件を満たさない場合のみ推計へフォールバック
- カードに**出自バッジを常時表示**（緑「実測（AIS受信）」／琥珀「AI推計（信頼度: 高/中/低）」）
- 推計時は**生成手段・推計根拠・信頼度**を注記で明示（編集方針 第3節に対応）
- ダーク船補正 1.35 の二重定義を解消し、表示側の定数1箇所に集約

### 実測0隻の扱い（3-C 三値化）

「取得成功かつ0隻」には「本当に船がいない」と「API 障害」の2つの原因があり、
前者を誤って握り潰すと封鎖の最重要シグナルを失い、後者を誤って表示すると重大な誤報になる。

**ホルムズが0隻だったときに限り対照海域（シンガポール海峡）を1コール叩き**、
そこに船がいれば真のゼロとして表示、対照も0なら API 障害として推計へ退避する。
通常時は追加コールを消費しない。

### データソース

**VesselAPI（vesselapi.com）無料枠・費用0円。** bbox は lat 25.8-27.0 / lon 55.6-57.0、
日次1回3コール＝月90コール（無料枠150）。実測21隻／うち移動中5隻（2026-09-03 実測）。
**船種を返さないため、内訳は「移動中／停泊・漂泊」**（従来のタンカー／貨物船は推計側に残る）。

### 残件

1. ~~**VesselAPI の規約回答待ち** — 公開サイトでの表示可否。2026-09-03 照会済み、
   **2026-09-17 までに返信がなければ規約を自ら読んで判断する**~~
   → 【2026-09-04 回答受領・解消】集計値の公開は許諾された（詳細は `hormuz-data-/tools/vesselapi-inquiry.md`「回答記録」）
2. ~~回答後に `update_ais.yml` を有効化（Secret `VESSELAPI_API_KEY` 登録＋`if: false` 削除）。
   **実 API 応答での動作確認は未了**のため、まず `workflow_dispatch` で1回試す~~
   → 【2026-09-05】**有効化しない**（カバレッジ不足で見送り）。`if: false` のまま維持。
   再開する場合に限り、この手順（Secret 登録・`if: false` 削除・`workflow_dispatch` で1回試す）を使う。
   実 API 応答でのワークフローの動作確認は未了のまま
3. ~~**90日後（2026-12-02 頃）に API キーを再発行**して Secret を更新する~~
   → 【2026-09-05】**使わないなら更新不要**。キーは 2026-12-02 に失効するが、アカウントは無料のため維持する。
   再開を決めた時点で再発行する（失効時は 401 でワークフローが失敗する設計）
4. 【2026-09-05 追加】**再開条件の確認**：四半期ごとに `hormuz-data-/tools/diag_vesselapi.py --count` を回し、
   東経 56.1 度以東の移動中隻数（カバレッジの変化）を見る。改善していれば採用を再検討する

### 一次情報

- `hormuz-data-/tools/README.md` — データソース選定の経緯・実測結果・制約
- `hormuz-data-/tools/vesselapi-inquiry.md` — 規約照会の記録
- ~~`hormuz-map/.claude/handovers/2026-09-03_2040.md`~~ → `hormuz-ops/handovers/2026-09-03_2040.md` — 実装の詳細・検証結果
- ~~`hormuz-map/.claude/handovers/2026-09-03_1430.md`~~ → `hormuz-ops/handovers/2026-09-03_1430.md` — 調査・方針合意の経緯
  （2026-09-04 に `hormuz-map/.claude/handovers/` を廃止し private リポジトリ `hormuz-ops` へ移設したため）

### 教訓

**外部データソースは、契約・実装の前にカバレッジを実測する。**
AISstream.io はこれを踏まなかったために4.5か月ぶんの誤データを生んだ。
`hormuz-data-/tools/diag_*.py` は次のソース選定でも再利用できる。

**自動更新の失敗は握り潰さず、ワークフローの失敗として表面化させる。**
「黙って0を書き続ける」状態は誰も気づけない。

---

## 日次更新の運用移行 — Claude Code 主体化（2026-09-02 着手）

### 背景

従来フローは「Claude.ai が `index_html_diffs.md` を生成 → 人が運搬 → Claude Code が適用」。
スマホ運用では **78KB の差分ファイルを手でコピペ**しており、これが最大の非効率かつ事故要因だった
（GitHub の自動提案コミットメッセージ `Change 'Hello World' to 'Goodbye World'` が
7/24〜9/2 でほぼ隔日・20件以上記録されていたのがその痕跡）。

### 決定事項

| 論点 | 決定 |
|---|---|
| スマホ運用 | Claude Code on the Web（クラウドセッション）へ移行。旧経路は当初「2〜3週凍結してから削除」としたが、2026-09-14 にバックアップとして保持に変更 |
| Claude.ai の役割 | 日次から外す。戦略・特別解説コラム執筆・週次レビューのみ。ただし初週は情報収集を並行させて WebSearch の品質を比較 |
| 完全自動化（無人公開） | **採用しない**。報道系のため「トリガーは人・作業は Claude Code・公開承認は人」の半自動が上限 |

### フェーズ

- **フェーズ0（完了・2026-09-02）**: 残骸ファイル11件の棚卸し、クラウド向け設定の整備
  - 続けて 2026-09-03（§11-8）に `docs/tools/` を廃止し、内部作業用ファイルを `tools/` へ移動。
    `apply_diffs.py` / `auto_push.py` / `daily-site-update` スキルの参照先も同時に修正済み（整合確認済み）
- **お試し（完了・2026-09-04）**: スマホのクラウドセッションで日次更新が完走。下記「お試し結果」参照
- **フェーズ2（完了・2026-09-12）**: `/daily-site-update` を Claude Code 完結型に書き換え、
  旧フロー（差分ファイル経由）を「凍結中」として同スキル内に隔離した
  - 実運用は 2026-09-06 に先行して移行済みで、スキルの記述だけが旧フローのまま残っていた
    （9/4 の日付がハードコードされた定型文も残存していた）ため、実態に合わせた
- **フェーズ1（完了・2026-09-12、2026-09-13 に基準日照合を追加）**:
  検証の自動化。`tools/validate_daily.py` を新設
  - 読み取り専用。日付整合7箇所・`latest` 4件と必須フィールド・`isLatest` の単一性・
    旧スキーマの混入・`archive_timeline` の日付重複を検査し、終了コードで成否を返す
  - **基準日そのものを実測日（JST）と照合する**（2026-09-13 追加）。
    未来日・2日以上前は NG、前日は WARN。
    きっかけは 2026-09-13 にユーザーが当日を「9月14日」と取り違えた件
    （記事内容に出てくる 9/14 サラーラ会合との混同。サイト自体は正しく 9/13 だった）。
    **ファイル間の整合だけでは、揃ったまま間違った日付で書かれた場合に気づけない**
    という穴が明らかになったため塞いだ。`datetime.now(UTC+9)` で計算するので
    クラウド（UTC）でも PC でも同じ結果になる
  - `--check-urls` で `latest` の URL を実際に叩く（捏造URL禁止ルールの機械的担保）
  - `daily-site-update` の「4. 自己チェック」と `publish-checklist` に組み込み済み。
    目視項目は「機械で判定できないもの」（文章の重複・内容の整合）に絞った
  - **JSON更新ヘルパー（`add_news.py` / `add_log.py`）は作らない判断とした。**
    クラウドセッションが JSON を直接編集する方式で 09-06 以降4回とも事故なく回っており、
    検証スクリプトが事故を事後に捕捉できるため、書き込み系ツールを増やす必要が薄い
- **フェーズ3（任意）**: 機械取得できるデータのみ Actions で定時取得

### news_data.json の過去データはスキーマが不揃い（2026-09-12 実測・一括修正は不要）

検証スクリプトを作る際に全件を調べた結果、**過去分と現行分でフィールド構成が違う**ことが分かった。

| 範囲 | 件数 | 全件に共通するフィールド |
|---|---|---|
| `latest`（現行） | 4 | 8個すべて揃っている（`id` `title` `body` `sourceLabel` `date` `label` `url` `isLatest`） |
| `osint` | 85 | `date` `isLatest` `url` のみ。古い分は `source` `summary` `title` という旧名を使っている |
| `archive.items` | 259 | `date` `title` `url` のみ。`body` `sourceLabel` `label` 等は一部のみ |

- **表示は現状で正常に動いている。**旧フィールドを読むコードが残っているか、
  該当箇所が描画対象外かのいずれか
- したがって**一括変換はしない。** `blockadeDay` のときと同じで、
  実害のない過去データを触ると破損リスクだけが残る
- `validate_daily.py` のスキーマ検査は **`latest`（当日更新される範囲）に限定**してある。
  ここを `osint` や `archive` へ広げると誤検知が大量に出るので広げないこと

### 移行後の定着状況（2026-09-12 時点）

**移行は実質完了している。** クラウドセッションからの日次更新が 09-06 / 08 / 10 / 12 と
4回連続で成功しており、いずれも `docs/index.html` と JSON 3本を**直接編集**している
（`tools/index_html_diffs.md` は一度も経由していない）。

旧経路は **2026-09-03 以降まったく使われていない**（実測）:

| 確認項目 | 結果 |
|---|---|
| `tools/index_html_diffs.md` の内容更新 | 0件（09-03 の移動が最後） |
| `mobile:` コミット（スマホ手編集） | 0件 |
| `Change 'Hello World' to...` の再発 | 0件 |
| `mobile-update.yml`（Actions）の実行 | 0件 |

### 旧経路ファイル — バックアップとして当面残す（2026-09-14 決定）

当初は「2〜3週間の凍結後に削除（目安 2026-09-17〜24）」としていたが、
**バックアップ経路として役立つ可能性があるため削除を見送り、様子を見る**ことにした。
期限は設けない。未使用であること自体は問題にしない。

```
tools/index_html_diffs.md / tools/diffs-generation-rules.md / tools/run.bat
auto_push.py / .github/scripts/apply_diffs.py / .github/workflows/mobile-update.yml
```

- バックアップとしての価値：`mobile-update.yml`（手動実行）＋`apply_diffs.py` の組み合わせは、
  **Claude Code を一切使わずに差分を本番へ適用できる唯一の経路**。
  クラウドセッションや Claude Code が使えない日の最終手段になる
- 将来削除するときは `auto_push.py` の `FILE_MAP` に `hormuz-data-` の `oil-flow.json` も
  入っている点に注意する（そちらを使い続けるなら `auto_push.py` は残す）
- ドメイン移行（~~2026-09-18 予定~~ → 2026-09-18 実施済み）後にこの経路を使う場合、**移行前の `index.html` を元に
  作った差分は `<head>` や JSON-LD 付近で `old_str` が一致せず適用に失敗する**。
  `apply_diffs.py` は完全一致しない差分をスキップするので壊れはしないが、
  Claude.ai 側の `index.html` の知識を移行後のものに更新してから差分を作ること

### 実測で判明した技術事実（重要）

- `.gitignore` が `.claude/settings.json` と `settings.local.json` の**両方**を除外しており、
  hook がクラウドに引き継がれていなかった → settings.json を追跡対象に変更済み
- **クラウド環境はシステム時刻が UTC**。素の `date` は JST と9時間ずれる。
  `TZ='Asia/Tokyo'` は Git Bash に tzdata がなく機能しない（UTCにフォールバック）。
  → `date -u -d '+9 hours'` が両環境で正しい唯一の方法
- ユーザーの `~/.claude/CLAUDE.md` はクラウドに引き継がれない
  → 応答ルール（日本語・結論先出し・確度提示）をプロジェクト CLAUDE.md に再掲済み
- クラウド環境のネットワークは既定 **Trusted**（GitHub・パッケージレジストリのみ）で
  報道各社に WebFetch できない → **Full に変更済み**（Custom との比較は別途検討）
- **クラウドセッションからは Claude in Chrome / コンピューター使用が利用できない**（PCと隔離のため）。
  ブラウザでの目視確認が要る作業（レイアウト崩れ確認等）は PC 作業として残る

### お試し結果 — ✅ 成功（2026-09-04 実施）

スマホから `/daily-site-update` を実行し、クラウドセッションでの日次更新が完走した
（`dc36adf` daily 更新 → `8af8641` main へマージ）。ユーザーの目視確認でも問題なし。

| 検証項目 | 結果 |
|---|---|
| クラウドで WebSearch が動くか | ✅ 動作。CENTCOM の 9/1 発表、ヴァンス副大統領の 9/3 発言、Gulf News / Al Jazeera の報道、原油価格まで反映されており、検索なしには書けない内容 |
| `[現在日時 JST]` の hook が注入されるか | ✅ 正常。全ての日付表記が 2026-09-04 11:40 JST で一致 |
| スマホでの差分レビュー・マージ | ✅ 実用に耐えた（ユーザー評価） |

機械検証でも整合を確認（後日フェーズ1でスクリプト化する項目の先取り）:
`dateModified` / `news_data.updated` / `update_log` 先頭 / `archive_timeline` 末尾 /
ヘッダー日時がすべて 9/4 11:40 JST で一致、`latest` 4件、`osint` の `isLatest` 1件。

### 解消済みの懸案

- ~~`news_data.json` の `latest` が 6件~~ → **2026-09-04 時点で 4件**。ルールどおりに戻っている
- ~~クラウドのコミット時刻が UTC（`+0000`）で記録される~~
  → **解消済み**。クラウド環境の Environment variables に `TZ=Asia/Tokyo` を追加した
  （2026-09-04〜06 に適用）。09-06 以降のクラウドコミットは全て `+0900` であることを実測で確認。
  クラウドは Ubuntu で tzdata を持つため、Windows の Git Bash と違い `TZ` が正しく効く。
  設定場所は環境セレクタ → クラウド → Default の歯車 → Environment variables

### 移行が安定してから着手すること

- **プロジェクトの運用マニュアルを作る**（2026-09-02 記録・着手時期は未定）
  - 分量はシンプルでよい。日々の運用手順が一望できる1枚を想定
  - **今は作らない。** 移行中は手順そのものが変わるため、書いた端から陳腐化する。
    運用フローが安定してから着手する
  - 判断の目安：旧経路（`index_html_diffs.md` / `run.bat`）の凍結解除がないまま
    2〜3週間クラウドセッションでの日次更新が回り、フェーズ2まで完了した時点
  - 進捗（2026-09-12）：**フェーズ1・2とも完了。凍結も9日間維持されている。**
    旧経路は削除せずバックアップとして保持に変更（2026-09-14）。着手条件は実質満たしている
    （ドメイン移行 9/18 と移行後の安定を待ってから着手するのが無難）
  - 【2026-09-19】ドメイン移行は 9/18 に完了。移行後の確認期間（〜10/2頃）の明けが着手の目安
    → 【2026-09-23】確認期間はゲート方式に変更。**着手の目安は §11-10 の再編の後**（再編で日次更新の経路とパスが変わり、手順が再び変わるため）
  - CLAUDE.md（ルール）・Memory.md（現状）・各 Project Skill（手順）と
    重複させない。マニュアルは「どれをいつ見るか」の入口に徹する

---

## 封鎖日数カウンター（blockadeDay）の起算日問題 — 調査結果と決定（2026-08-07）

### 結論

**`archive_timeline.json` は一切変更せず、`docs/archive/index.html` 側で日付から動的計算する方式を採用した。**
封鎖日数の起算日は **2026年2月28日を「1日目」** とする（2026-08-05 = 159日目）。

### 調査の経緯

2026-08-01時点のメモリーには「`archive_timeline.json` の既存エントリーが
4/13起算で計算されている可能性があり、後日まとめて全件を再計算する」と記録されていた。
2026-08-07に全142エントリーを実データ検証した結果、**この想定は誤りだった**ことが判明した。

### 実データ検証で判明した事実（全142エントリー）

| 起算日 | 該当範囲 | 件数 | 備考 |
|---|---|---|---|
| **4/13**（二重封鎖＝米港湾封鎖の開始日） | 4/16 のみ | **1件** | `blockadeDay: 3`。想定に反し1件のみだった |
| **2/27** | 4/30〜6/24 | 22件 | 現行基準比 +1 |
| **2/28**（現行の正） | 6/25以降 | 37件 | 正しい |
| （未設定 `null`） | 全期間に散在 | 82件 | スタンプが非表示になっていた |

- **6/24 と 6/25 がともに `118`** になっているのは、カウンタを1日据え置いて
  起算日を 2/27 → 2/28 に再同期した痕跡。**単純ミスではなく意図的な定義変更**である。
- **6/3 の `102`** のみ、旧基準（2/27起算なら97）でも説明できない単純入力ミス。
- JSONの `blockadeDay` と速報本文中の「封鎖◯日目」は **57/57件で完全一致**していた。

### 一括上書きを採用しなかった理由

1. **JSONと本文が100%一致しているため、JSONのみ修正すると
   「現時点で存在しない矛盾」を23件、新規に発生させる**ことになる。
2. 本文も含めて書き換えると、**過去に実際に公開した速報本文の改変**にあたる。
   起算日の変更は「当時の定義では正しかった数字」であり、誤記ではない。
3. 142件のフル上書きはデータ破損リスクが高い。

### 採用した解決策（2026-08-07 適用・push済み）

`docs/archive/index.html` に以下を追加し、`e.blockadeDay` への参照を全廃した。

```javascript
// 封鎖日数の起算日：2026年2月28日を「1日目」とする（月は0始まりのため 1 = 2月）
const BLOCKADE_BASE_UTC = Date.UTC(2026, 1, 28);
function calcBlockadeDay(iso){
  const [y, m, d] = iso.split('-').map(Number);
  return Math.round((Date.UTC(y, m - 1, d) - BLOCKADE_BASE_UTC) / 86400000) + 1;
}
```

**変更箇所（計4点）：**
- 関数定義の追加（`fmtJpDate()` の直後）
- `renderStats()` の統計欄：`entries.map(e => e.blockadeDay).filter(Boolean)` → `entries.map(e => calcBlockadeDay(e.date))`
- 各エントリーのスタンプ：条件分岐を撤去し `calcBlockadeDay(e.date)` を常時表示
- `renderRangeNotice()` に起算日の注記を追加

**効果：**
- 統計欄の異常表示 `3〜159日目` → `1〜159日目` に正常化
- スタンプ欠落82件が解消し、全142件に表示
- データ破損リスク・本文改変ともにゼロ
- 将来のエントリーで `blockadeDay` の記入精度を問う必要がなくなった

### 残存する既知の差異（許容）

23件のエントリーで「スタンプの数字」と「本文中の数字」が1日ずれる。
これは `renderRangeNotice()` に追加した以下の注記で読者に説明している。

> 封鎖日数は2026年2月28日を1日目として再計算した値を表示しています。
> 2026年6月24日以前の速報本文中の「封鎖◯日目」という表記は、
> 当時採用していた起算日（2月27日）に基づくため、
> スタンプの数字と1日ずれている場合があります。

### 運用上の教訓（重要）

**「〜の可能性がある」という推測をメモリーやドキュメントに残すと、
後日それが「確定した事実」として扱われ、誤った一括処理を招くリスクがある。**

2026-08-01に「4/13基準で汚染されている可能性」を実データ未検証のまま記録した結果、
本来不要かつ有害な一括修正が「予定タスク」として残り続けていた。

→ **対策：修正作業に着手する前に、必ず実データを検証してから方針を決定すること。
  推測を記録する場合は「未検証の仮説」であることを明示すること。**

---

## 完了済み主要作業

- ✅ フェーズ2（CSS のみ：2-a 文字サイズ・2-b 行の長さ・2-c カラム／ブレークポイント統一）を本番反映（2026-09-18 / `12f3d7e` `76e297b` `7d154e3` `eb488ad` ほか）
  - HTML は変えず `docs/index.html` の `<style>` 内のみ。詳細・10/2 以降に回した残りは `tools/redesign-plan.md` §4
- ✅ 独自ドメイン移行（2026-09-18 / `75bbdac` Custom domain・`60b62a1` ホスト名置換）
  - 本番検証は基準値どおり（`tools/migration-baseline-2026-09-13.md` §8）。旧 URL は 301 転送
- ✅ AIS 実測と推計の分離・VesselAPI 移行（2026-09-03 / `hormuz-data-` `c8f9636`）
  - 1ファイルに2つの生成元がある状態を解消。読者に実測か推計かが分かる表示へ是正
  - ~~有効化のみ規約回答待ちで保留（上記セクション参照）~~ → 2026-09-05 にカバレッジ不足で有効化を見送り。
    当面は AI 推計のみで運用（上記セクション参照）
- ✅ 運用移行フェーズ0：残骸ファイル11件の棚卸し・クラウド向け設定整備（2026-09-02）
  - `/docs` 再編（2026-06-02 `fc2807f`）で置き去りになった `data/news_data.json` や
    適用済みパッチ類を削除。うち5件は `docs/` 配下のため GitHub Pages で公開状態だった
  - hook・応答ルール・permission をリポジトリ側に移し、クラウドセッションへ引き継ぎ可能にした
- ✅ サブエージェント作成（mobile-ui-reviewer / responsive-css-specialist）（2026-05-07）
- ✅ デザイン基準書・スマホUIルール作成（tools/design-system.md / tools/mobile-ui-rules.md）（2026-05-07）
- ✅ CLAUDE.mdにスマホUI改善ルール・専門エージェント使い分けを追記（2026-05-07）
- ✅ Memory.md 新規作成・Claude.aiプロジェクト知識連携（2026-05-06）
- ✅ スマホフォントサイズ底上げ（`@media (max-width:768px) { html { font-size:18px } }`）（2026-05-06）
- ✅ 速報インシデント・関連最新ニュース・現地メディア視点 折り畳み整備（各3件表示統一）（2026-05-06）
- ✅ 現地メディア視点 LIMIT 4→3 に変更（2026-05-06）
- ✅ ホルムズ海峡危機マップ 公開・毎日更新運用中
- ✅ news_data.json による動的ニュースレンダリング実装
- ✅ auto_push.py / run.bat による半自動push体制構築
- ✅ syncScenarioFromDashboard() によるシナリオ確率自動同期
- ✅ インフォグラフィック6枚 公開
- ✅ MAP凡例折り畳み（スマホ対応）
- ✅ ルートテーブル7列構成 + 日本フロー列
- ✅ 2026年5月5日分まで更新完了

---

## 環境メモ

- **OS**: Windows 11（Macなし）
- **ffmpeg パス**: `C:\ffmpeg\bin\ffmpeg.exe`（PATH未設定環境対応）
- **Python**: venv 使用 → `venv\Scripts\python`
- **Claude.ai**: 戦略・特別解説コラム・週次レビュー（2026-09-02 の決定により日次からは外す方針。移行完了までは従来どおり差分ファイル生成も担う）
- **Claude Code（PC）**: index.html更新・commit（pushはユーザー確認後）
- **Claude Code（クラウド／スマホ）**: 移行先。ネットワークは Full 設定済み。
  ブラウザ操作・コンピューター使用は PC と隔離のため利用不可
- **auto_push.py / run.bat**: DownloadsフォルダからGitHub API直接push（旧経路・~~凍結予定~~ → 2026-09-14 に**バックアップとして保持**に変更。期限なし）

---

## セッション再開コマンド

```
前回のセッションを再開して
```
